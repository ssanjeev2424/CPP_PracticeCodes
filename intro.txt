
* Go to folder where u wanna copy the repo, then write 
	"git clone copied_clone_link;"

* "git status;" after going to the directory tells whether our current 		version is same as master version on git or not.

* if any changes or new file is there in ur system, write 
	"git add file_name";

* write "git commit -m "commitMessage";
* write "git push"
* It will for username and password. change will be committed to github

* if we have any changes in Git code, we can go to our git directory, (after cloning), write "git pull", it will copy those contents..

* "git add -A" will add all the files to the git            "escape : wq"
